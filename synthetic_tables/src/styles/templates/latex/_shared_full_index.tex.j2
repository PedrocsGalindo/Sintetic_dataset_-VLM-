\clearpage
\section*{Detailed Tables}
\noindent\textit{Detailed row content starts after the overview page. Every section repeats the Record anchor so the PDF remains traceable back to the generated source rows.}

{% for section in latex.detail_sections %}
\subsection*{ {{ section.title_escaped }} }
\noindent\textit{ {{ section.subtitle_escaped }} }

{{ section.font_command }}
\rowcolors{2}{HeaderFill!10}{white}
\begin{longtable}{ {{ section.column_spec }} }
\toprule
{% for column in section.columns -%}
{{ column.header_escaped }}{% if not loop.last %} & {% endif %}
{%- endfor %} \\
\midrule
\endfirsthead
\toprule
{% for column in section.columns -%}
{{ column.header_escaped }}{% if not loop.last %} & {% endif %}
{%- endfor %} \\
\midrule
\endhead
{% for row in section.rows -%}
{% for cell in row -%}
{{ cell }}{% if not loop.last %} & {% endif %}
{%- endfor %} \\
{% endfor %}
\bottomrule
\end{longtable}
\normalsize
{% endfor %}

% PDF_FALLBACK_TABLE_BEGIN
% {{ latex.full_index_font }}
% \rowcolors{2}{HeaderFill!10}{white}
% \begin{longtable}{ {{ latex.full_index_column_spec }} }
% \toprule
% {{ columns | map(attribute='header_escaped') | join(' & ') }} \\
% \midrule
% \endfirsthead
% \toprule
% {{ columns | map(attribute='header_escaped') | join(' & ') }} \\
% \midrule
% \endhead
{% for row in rows -%}
% {{ row | join(' & ') }} \\
{% endfor %}
% \bottomrule
% \end{longtable}
% \normalsize
% PDF_FALLBACK_TABLE_END
